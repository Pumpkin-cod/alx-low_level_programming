#include "main.h"
/**
 * _isupper - Prints  uppercase characters.
 * @c: Checks the code.
 * Return: 1 otherwise 0.
 */
int _isupper(int c)
{
	if
	(c >= 'A' && c <= 'Z')
	return (1);
	else
	return (0);
}
