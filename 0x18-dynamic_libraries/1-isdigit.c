#include "main.h"
/**
*_isdigit - Checks for charaters that are digits
*@c:Characters
*Return:1 if it is a digit otherwise 0.
*/
int _isdigit(int c)
{
	if
	(c >= '0' && c <= '9')
	return (1);
	else
	return (0);
}
